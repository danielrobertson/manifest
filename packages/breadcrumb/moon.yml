type: 'library'
language: 'typescript'

workspace:
  inheritedTasks:
    rename:
      buildPackage: 'build'

tasks:
  build:
    outputs:
      - 'esm'
      - 'lib'

dependsOn:
  - id: 'icons'
    scope: 'production'
  - id: 'link'
    scope: 'production'
  - id: 'styles'
    scope: 'production'
  - id: 'types'
    scope: 'development'
  - id: 'use-merged-ref'
    scope: 'production'
  - id: 'utils'
    scope: 'production'
